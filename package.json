{
  "name": "life",
  "version": "1.0.0",
  "description": "Desktop wallpaper app for macOS — calendar, weather, running stats, football scores, Pomodoro timer",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && esbuild src/renderer/dashboard.ts --bundle --platform=browser --outfile=dist/renderer/dashboard.js && cp src/renderer/index.html dist/renderer/index.html",
    "start": "npm run build && electron .",
    "dev": "tsc -w",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^25.5.2",
    "electron": "^41.2.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.2",
    "vitest": "^3.2.4"
  }
}
