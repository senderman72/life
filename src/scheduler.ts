import type { BrowserWindow } from 'electron'
import type { AppConfig } from './types'

export function startScheduler(_win: BrowserWindow, _config: AppConfig): void {
  // Intervals wired up in subsequent phases
}
